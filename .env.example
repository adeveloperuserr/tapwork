# Database
DATABASE_URL=postgresql+asyncpg://tapwork:tapwork@localhost:5432/tapwork

# Security (Genera una clave segura: python -c 'import secrets; print(secrets.token_urlsafe(32))')
SECRET_KEY=change-this-secret
ENVIRONMENT=development

# CORS (Separar múltiples orígenes con comas: http://localhost:3000,https://app.com)
ALLOWED_ORIGINS=*

# Token expiration (en minutos)
ACCESS_TOKEN_EXP_MINUTES=1440
EMAIL_VERIFICATION_TOKEN_EXP_MINUTES=1440
PASSWORD_RESET_TOKEN_EXP_MINUTES=60

# Email/SMTP
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_TLS=false
MAIL_FROM=noreply@example.com

# URLs
FRONTEND_BASE_URL=http://localhost:3000
API_BASE_URL=http://localhost:8000

# Features
ENABLE_BIOMETRIC=false

# Admin user (creado al ejecutar seed.py)
ADMIN_EMAIL=adeveloper.user@gmail.com
ADMIN_PASSWORD=aDeveloperUser2025$
